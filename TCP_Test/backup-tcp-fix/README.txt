Snapshot of the corrected (real-TCP / TCP Reno) implementation.

Taken from the working tree after the TCP-principle correction (2026-08-16).
These are the CURRENT files. The old (mimic) version is in ../backup-current-version/.
Restore with: cp backup-tcp-fix/*.java src/com/ouc/tcp/test/
Then recompile (Java 8): see AGENTS.md.
